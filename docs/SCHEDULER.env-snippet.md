# Minutes without a successful worker pass before health reports the scheduler stale.
# See docs/SCHEDULER.md. Default 15. Prefer an external pinger for punctual /api/cron/queue.
FULLSEND_WORKER_STALE_MINUTES=15
